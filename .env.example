# Provider selection is normally saved with:
#   python -m app.main provider select
# You can still override it for automation with AI_PROVIDER=openrouter or codex.
AI_PROVIDER=

OPENROUTER_API_KEY=your_openrouter_api_key_here
# Must support image input because both agents receive SDK input_image payloads.
OPENROUTER_MODEL=qwen/qwen3.6-plus
OPENROUTER_AGENT_MAX_TURNS=30
# Extra bounded turns for a final "save and stop" pass after the coder hits OPENROUTER_AGENT_MAX_TURNS.
OPENROUTER_AGENT_FINISH_TURNS=5

# Optional model generation settings.
# Temperature range: 0.0-2.0. Blank uses OpenRouter/model defaults.
OPENROUTER_TEMPERATURE=
# Reasoning effort: none, minimal, low, medium, high, or xhigh. Blank uses defaults.
OPENROUTER_REASONING_ENABLED=
OPENROUTER_REASONING_EFFORT=
# Set true to let the model reason internally without returning reasoning tokens.
OPENROUTER_REASONING_EXCLUDE=

# Optional: override the shared model per agent.
# OPENROUTER_CODER_MODEL=openai/gpt-4o
# OPENROUTER_EVALUATOR_MODEL=openai/gpt-4o

# Optional Codex OAuth mode settings. Credentials are managed with:
#   python -m app.main auth codex login
#   python -m app.main auth codex import
# Model values are required when AI_PROVIDER=codex or the saved provider is codex.
CODEX_MODEL=
# CODEX_CODER_MODEL=
# CODEX_EVALUATOR_MODEL=
CODEX_BASE_URL=https://chatgpt.com/backend-api/codex
# CODEX_AUTH_FILE=
# CODEX_HOME=

# Optional OpenRouter metadata.
OPENROUTER_HTTP_REFERER=http://localhost
OPENROUTER_APP_TITLE=IBM Hackathon Agent Workflow

# Optional OpenRouter provider routing.
# Leave these empty to use OpenRouter's default provider settings.
OPENROUTER_PROVIDER_ORDER=
OPENROUTER_PROVIDER_ONLY=
OPENROUTER_PROVIDER_IGNORE=
OPENROUTER_PROVIDER_ALLOW_FALLBACKS=
OPENROUTER_PROVIDER_REQUIRE_PARAMETERS=
OPENROUTER_PROVIDER_DATA_COLLECTION=
OPENROUTER_PROVIDER_ZDR=
OPENROUTER_PROVIDER_ENFORCE_DISTILLABLE_TEXT=
OPENROUTER_PROVIDER_QUANTIZATIONS=
OPENROUTER_PROVIDER_SORT=

# Optional: enables MiniMax MCP web/search tools for the coder agent.
MINIMAX_API_KEY=
MINIMAX_API_HOST=https://api.minimax.io
