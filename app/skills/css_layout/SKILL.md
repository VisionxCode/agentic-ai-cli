# CSS Layout

Use stable CSS dimensions and responsive constraints. Prefer grid and flexbox for repeated structures. Avoid layout shifts caused by dynamic content. Match the fixed screenshot viewport before optimizing for other sizes.

