# SVG Generation

Use inline SVG for vector shapes that need exact curves, icons, charts, logos, or illustrations. Keep SVG dimensions explicit and ensure it scales predictably inside the HTML layout.

